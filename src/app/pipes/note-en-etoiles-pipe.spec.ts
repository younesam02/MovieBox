import { NoteEnEtoilesPipe } from './note-en-etoiles-pipe';

describe('NoteEnEtoilesPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteEnEtoilesPipe();
    expect(pipe).toBeTruthy();
  });
});
